<!DOCTYPE html>
<html lang="da">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Note: <%= note.title %></title>
  <style>
    body { margin:0; font-family:Arial,sans-serif; background:#fafafa; color:#333; }
    .container { max-width:600px; margin:3rem auto; padding:1rem; background:#fff; border-radius:0.5rem;
      box-shadow:0 2px 6px rgba(0,0,0,0.1);
    }
    h1 { margin-top:0; font-size:1.8rem; }
    .meta { color:#555; margin-bottom:1.5rem; font-size:0.9rem; }
    .content { white-space:pre-wrap; line-height:1.6; }
    a.back {
      display:inline-block; margin-top:1.5rem; color:#0077cc; text-decoration:none;
    }
    a.back:hover { text-decoration:underline; }
  </style>
</head>
<body>
  <div class="container">
    <h1><%= note.title %></h1>
    <div class="meta">
      Kategori: <strong><%= note.category %></strong><br>
      Oprettet: <%= note.createdAt.toLocaleDateString('da-DK', { day:'2-digit', month:'2-digit', year:'numeric' }) %>
    </div>
    <div class="content"><%= note.content %></div>
    <a class="back" href="/psychologist/patients/<%= patientId %>">
      ← Tilbage til patient
    </a>
  </div>
</body>
</html>
